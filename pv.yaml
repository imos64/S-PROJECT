apiVersion: v1
kind: PersistentVolume
metadata:
  name: mysql0
  namespace: dev-ahyeha
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: manual
  capacity:
    storage: 50Gi
  hostPath:
    path: /mysql1

---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: mysql1
  namespace: dev-ahyeha
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: manual
  capacity:
    storage: 50Gi
  hostPath:
    path: /mysql2
